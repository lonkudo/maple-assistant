# Maple 助手 — 安装与分辨率指南

## 1. 安装（给最终用户）

项目可以一键自举安装，无需任何 Python 知识。

1. 解压发布压缩包：右键 zip →「全部解压」→ 打开解压后的文件夹。
   ⚠️ 一定要先解压！在压缩包窗口里直接双击是不会运行的。
2. **双击 `安装.bat`** —— 唯一需要做的操作，全程自动：
   - 安装程序一开始只弹出一次 **UAC 管理员权限确认**，点击「是」后其余步骤自动完成。
   - 如果弹出「打开文件 - 安全警告」或「Windows 已保护你的电脑」，
     点击「仍要运行 / 运行」即可。
   - 脚本会查找本机已有的 Python 3.10–3.12（**优先 3.10**）；
     找不到时会从 python.org **后台静默安装 Python 3.10**，不会显示 Python
     安装向导；失败时再用 winget 静默重试。
   - 创建本地 `.venv` 并安装全部依赖：基础库（numpy、Pillow、OpenCV、
     pywin32）+ **YOLO 怪物检测依赖**（CPU 版 PyTorch、mss、ultralytics 等，
     装进同一个主环境，无需单独的 venv313）。**pip 优先走阿里云镜像，
     镜像失败时自动回退官方 PyPI**，
     国内下载更快。
   - 自动生成两个启动器：**`start_assistant.bat`** 和 **`启动助手.bat`**
     （使用 Python 3.10 解释器启动助手）。
3. 把训练好的模型放到 `yolo-detection\weights\best.pt`（YOLO 检测用）。
4. 双击 **`启动助手.bat`** 启动。游戏窗口在前台时点击 **开始巡逻** 即可。
   - 启动时会弹出 **UAC 管理员权限确认**，请点击「是」——助手需要与游戏
     相同的权限才能把按键注入游戏（否则能检测到游戏窗口、能看到按键事件，
     但角色不会动）。
   - **游戏也必须以管理员权限运行**（右键游戏 →「以管理员身份运行」），
     否则按键依然无法注入。两者权限一致是按键生效的前提。
   - 启动器通过隐藏的 `wscript` 和 `pythonw` 启动；UAC 确认后不会留下或反复
     弹出命令行窗口。窗口查找与前台检查只在点击「开始巡逻」后运行。

> UAC 后只需等待安装命令行窗口跑完；Python 安装器不会另外显示向导。
> 缺少依赖时，重新双击 `安装.bat` 即可补装。

## 1.1 攻击模式与 YOLO 检测

- **攻击模式** 面板（标题为「攻击模式」）统一设置自动攻击行为：
  - **YOLO 检测**：开始巡逻时**自动启动 YOLO 检测并自动攻击**，攻击按键
    使用本面板设置的按键；
  - **固定攻击**：按设定间隔自动按攻击按键。
- **YOLO 怪物检测** 面板不再有「自动攻击」开关：检测/攻击行为全部由
  「攻击模式」面板控制。「显示检测画面」为可选项，**默认不显示**；
  勾选后再启动检测会弹出实时画面窗口。

## 2. 支持的分辨率

所有分析都**以游戏客户区为基准归一化**，因此会随你游玩的分辨率自动适配：

| 分辨率 | 比例 | 支持情况 |
|---|---|---|
| 2560×1440 | 16:9 | ✓ 支持 |
| 1920×1080 | 16:9 | ✓ 支持 |
| 1366×768 | 16:9 | ✓ 支持 |
| 2560×1600 | 16:10 | ✓ 支持（原始校准分辨率） |

原理：

- 截图改为抓取**整个客户区**，所有分析区域（小地图、HP/MP 条）都是客户区的
  归一化比例。小地图通过 OpenCV 轮廓动态定位，巡逻点以菱形相对偏移存储，
  HP/MP 条宽度以客户区宽度为基准——因此都与你的分辨率无关。
- YOLO 检测器**自动匹配游戏窗口的真实客户区**（每几秒刷新一次，窗口移动/
  缩放后自动跟随），不再依赖写死的 config.yaml 尺寸；只有找不到游戏窗口时
  才回退到 `yolo-detection\config.yaml` → `window.default`。
  检测区宽度/高度、**攻击范围**、**最小怪物尺寸**全部是**相对比例**
  （% 窗口宽度），运行时按实际窗口尺寸换算成真实像素——换分辨率/拉窗口
  自动跟随，无需重新设置（UI 选择会保存到 `user_config.json`）。

**每个分辨率只需做一次**（切换分辨率会改变 UI 缩放比例）：

1. 确认小地图能被检测到（界面上会显示小地图预览和方框）。
2. 确认状态栏里的 HP/MP 数值正常；如果偏差约 10%，请在
   `status_worker.py` 中调整条宽比例
   （`full_bar_width_fraction`，取值 = 满血条像素 ÷ 客户区宽度）。
3. 如果重新录制巡逻点，请在**将要使用的分辨率下录制**——菱形相对存储让
   坐标可以跨分辨率使用，但在目标分辨率下重新录制最准确。

## 3. 常见问题

- **提示缺少模块 "No module named X"** — 虚拟环境不完整：重新双击 `安装.bat`。
- **开始巡逻时游戏窗口选择失败** — 助手不会发送 Alt 键（Alt 是跳跃键），
  聚焦依靠直接 `SetForegroundWindow`；请在游戏窗口可见时点击开始巡逻。
- **开始巡逻时角色会跳一下** — 此问题已修复（窗口选择不再按 Alt + 开始
  缓冲期）；若仍出现，请检查 `patrol_start_grace_seconds` / `alt_transition`
  设置。
- **能看到按键事件但角色不动** — 权限不一致：助手以管理员运行时，游戏也必须
  「以管理员身份运行」。启动助手.bat 会自动弹出 UAC 确认；若游戏未以管理员
  运行，注入的按键会被系统丢弃。
- **提示"地图名字不同 / map name does not match"** — 说明当前画面和已录制的地图
  不一致。默认配置**不绑定任何地图**（`user_config.json` 的 recording
  地图名为空）：请先在当前地图上录制 最左/绳索/最右，
  地图校验才会生效；换地图后请「重置录制」重新录制。
- **YOLO 提示缺少运行环境** — 重新双击 `安装.bat` 自动安装全部依赖
  （装到主环境 .venv，无需单独的 venv313）。
- **YOLO 进程启动后很快退出** — 多半是依赖没装全，重新双击 `安装.bat`
  补装；仍不行就把 `yolo-detection\yolo_launch.log` 发给开发者。

## 4. 安全声明

自动化可能违反游戏服务器的规则，请仅在允许的场合使用。
