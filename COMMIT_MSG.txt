fix: per-frame layer tracking (also during drop/return and the climb Up-hold);
recover Y_TOLERANCE (drop the x0.75 detection-time taper)

- run loop: _resync_route_layer now runs EVERY frame - the suppression gate
  during the drop/return is removed, so the floor is re-read and tracked
  every frame even mid-drop.  The flicker guard still keeps the current
  patrol floor while the marker Y stays inside its own band, so an
  intermediate platform (e.g. the bench mid-approach) cannot hijack the
  drop/return and restart a middle floor's patrol.
- climb arrival Up-hold: the fixed hold now ALSO re-reads the floor every
  frame (tracked + logged) so the bench -> rope -> next-layer transition is
  followed frame by frame, without switching the hold itself mid-flight
  (that made the character fall off the rope).
- Y_TOLERANCE recovered: the recorded tolerance is used as-is in the band
  math again - the x0.75 detection-time taper constant and its scale line
  are removed from movement_worker and patrol_control (bands stay
  (topmost - tolerance, lowermost) per the earlier rule).
- test comment updated back to the recovered band edge (0.4968 .. 0.5168).