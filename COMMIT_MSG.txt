fix: debug UI never steals foreground and remembers its window position/size

- run(): remove root.attributes('-topmost') - the startup raise pulled the
  debug UI in front of the game mid-climb, so the game lost focus, held keys
  (up/right/z) were released and the character jumped off the rope.  The UI
  also no longer opens pinned at +0+0: it restores the last saved geometry.
- add pure geometry helpers (_parse_window_geometry / _clamp_window_geometry
  / _load_window_geometry / _save_window_geometry) persisted to
  ui_window_settings.json (periodic save + save on close), so the window is
  movable/adjustable and is clamped back on-screen when a saved position
  went off-screen (e.g. after a monitor change).
- test_ui_worker: WindowGeometryHelperTests covering parse, on-screen
  clamp, minimum size, settings roundtrip and corrupt/missing fallback.